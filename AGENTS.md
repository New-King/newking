# 项目约定（Agent 必读）

## 分派（按需读对应文档，不要每次全读）
- **改前端（`src/`）** → 先读 `docs/前端规则.md`（UI/文案/项目页/易错点）+ `docs/设计规范.md`。
- **改后端（`backend/`）** → 先读 `backend/README.md`（架构/检索策略/SSE/调试/常见问题）。
- 项目介绍 / 技术栈 / 快速开始 → 看 `README.md`。

## 技术方案守则（硬性：遇问题先想成熟方案，禁止乱打补丁）
- 遇到 bug / 缺陷时，**先停下来判断根因**，不要在原方案上堆补丁。
- 自问：这个问题的**业界成熟做法**是什么？当前方案是不是一开始就选错了？
  （例：id 唯一性用 UUID 而非自增计数器；会话回显用服务端 id 或 UUID，不靠全局计数器+持久化时序。）
- 每次加代码前问自己：**这段逻辑是否已有现成/更简单/更标准的实现？**
- **重大方向性 / 架构性修复，必须先向用户说明「根因 + 业界方案 + 我的推荐」，征得同意后再动手**，不擅自改。
- 小修复可先做，但若发现"同一处修补超过 2 次还在报错"，立即停下重新审视整体方案。

## 开发协作守则（通用，务必遵守）
- **复杂交互先出方案再写码**：涉及流程动画 / 布局切换 / 交互样式时，先给出「布局示意 + 时间线/交互说明」文字稿，用户确认后再实现，不得直接动手猜。
- **用户给示意图 / 参考图 / 红线标注时，严格照做**，不得自创变体；有歧义先问清再实现。
- **主观形容词（大气 / 隐约 / 直观 / 高级）必须追问到可执行参数**，或直接给 2-3 个 A/B 方案让用户选，禁止猜完再改。
- **Git 操作需确认（硬性）**：commit / push 一律在用户明确要求后进行；任务完成需要提交时，先汇报结果并询问，不得擅自提交或推送。
- 复杂功能小步提交（每轮确认后提交），方便回退。

## 对话调试
对话 Agent 回答质量差时，按 `backend/README.md` 的「对话调试」章节定位，**禁止凭猜乱改**（先复现 → 看 LangSmith 轨迹 → 分环节验证 → 环境检查 → 改后跑场景矩阵）。

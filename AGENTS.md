# 项目约定（Agent 必读）

## 分派（按需读对应文档，不要每次全读）
- **改前端（`src/`）** → 先读 `docs/前端规则.md`（UI/文案/项目页/易错点）+ `docs/设计规范.md`。
- **改后端（`backend/`）** → 先读 `backend/README.md`（架构/检索策略/SSE/调试/常见问题）。
- 项目介绍 / 技术栈 / 快速开始 → 看 `README.md`。

## 技术方案守则（硬性：先想成熟方案，禁止乱打补丁）
- 遇问题先判断**根因**，再决定修法；不要在原方案上反复堆补丁。若同一处已修超过 2 次仍未解决，停下重新审视整体方案。
- 动手前自问：这个问题的**业界成熟做法 / 现成实现**是什么？当前方案是否一开始就选错了？是否有更简单、更标准的做法？
- **用不熟悉的库 / API / 框架前，先查其版本文档与正确用法**，禁止凭旧经验或猜测直接写；写错后更不能在错误假设上继续打补丁。
  若对版本 / 用法拿不准，或查到的文档与实际不符时，**向用户确认**（必要时让用户提供正确文档 / 示例 / 仓库）。
- **重大方向性 / 架构性修复，必须先向用户说明「根因 + 业界方案 + 我的推荐」，征得同意后再动手**，不擅自改。

## Git操作守则
- **禁止主动执行代码的 commit / push操作**，一律在用户明确要求后进行

## 对话调试
对话 Agent 回答质量差时，按 `backend/README.md` 的「对话调试」章节定位，**禁止凭猜乱改**（先复现 → 看 LangSmith 轨迹 → 分环节验证 → 环境检查 → 改后跑场景矩阵）。
